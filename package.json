{
  "name": "@rwillians/pg",
  "description": "Managed PostgreSQL made easy.",
  "version": "18.0.1",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "engines": {
    "bun": "^1.3.9"
  },
  "scripts": {
    "typecheck": "bun run --bun tsc --noEmit",
    "test": "bun test src/**/*.spec.ts",
    "compile": "bun build --compile --outfile=dist/pg src/cli.ts && chmod +x dist/pg"
  },
  "dependencies": {
    "@rwillians/qx": "^0.2.0",
    "arkregex": "^0.0.5",
    "croner": "^10.0.1",
    "telegraf": "^4.16.3",
    "yargs": "^18.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/yargs": "^17.0.35"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
