{
  "name": "@rwillians/pg",
  "description": "Managed PostgreSQL made easy.",
  "version": "18.0.0",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "engines": {
    "bun": "^1.3.9"
  },
  "scripts": {
    "typecheck": "bun run --bun tsc --noEmit",
    "pretest": "bun run --bun tsc --noEmit",
    "test": "bun test src/**/*.spec.ts",
    "precompile": "bun run --bun tsc --noEmit && bun test src/**/*.spec.ts",
    "compile": "bun build --compile --outfile=dist/pg src/cli.ts && chmod +x dist/pg"
  },
  "dependencies": {
    "@rwillians/qx": "^0.1.21",
    "arkregex": "^0.0.5",
    "yargs": "^18.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/yargs": "^17.0.35"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
